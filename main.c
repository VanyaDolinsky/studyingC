#include <stdio.h>
#include <math.h>
 
long int fact(int i);
int main(void) {
	int n, a;
	printf("Input n:");
	scanf("%d", &n);
	printf("Input a:");
	scanf("%d", &a);
	printf("Factorial n!=%ld\n", fact(n));
return 0;
}
	long int fact(int i) 
	{
	}
}
